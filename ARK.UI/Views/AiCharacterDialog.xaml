<Window x:Class="ARK.UI.Views.AiCharacterDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:res="clr-namespace:ARK.UI.Resources"
        Title="ARK — AI"
        Width="500"
        SizeToContent="Height"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Themes/Brushes.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/Themes/Buttons.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/Themes/Inputs.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- Метка поля: Consolas, 9pt, #888 -->
            <Style x:Key="FieldLabel" TargetType="TextBlock">
                <Setter Property="Foreground"  Value="#FF888888"/>
                <Setter Property="FontSize"    Value="9"/>
                <Setter Property="FontFamily"  Value="Consolas"/>
                <Setter Property="FontWeight"  Value="Bold"/>
                <Setter Property="Margin"      Value="0,0,0,5"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <!-- Внешний отступ: место для GoldGlowEffect (тень не обрезается краем окна) -->
    <Border Margin="10">
        <Border Background="{StaticResource ObsidianBackgroundBrush}"
                BorderBrush="{StaticResource GoldBrush}"
                BorderThickness="1.5"
                CornerRadius="8"
                Effect="{StaticResource GoldGlowEffect}">

            <StackPanel Margin="16,12,16,16">

                <!-- ── Заголовок + кнопка закрыть ── -->
                <Grid Margin="0,0,0,16"
                      MouseLeftButtonDown="OnTitleBarDrag"
                      Cursor="SizeAll">
                    <TextBlock Text="{x:Static res:Strings.AiCharacter_Title}"
                               Foreground="{StaticResource GoldBrush}"
                               FontSize="13"
                               FontWeight="SemiBold"
                               FontFamily="Segoe UI"
                               VerticalAlignment="Center"/>
                    <Button Style="{StaticResource CloseButtonStyle}"
                            HorizontalAlignment="Right"
                            Width="24" Height="24"
                            Padding="4"
                            FontSize="12"
                            Content="✕"
                            Click="OnCancelClick"/>
                </Grid>

                <!-- ── Имя ассистента ── -->
                <TextBlock Text="{x:Static res:Strings.AiCharacter_NameLabel}"
                           Style="{StaticResource FieldLabel}"/>
                <TextBox x:Name="NameBox"
                         Foreground="#FFF5D77F"
                         FontFamily="Segoe UI"
                         FontSize="13"
                         Margin="0,0,0,14"
                         KeyDown="OnKeyDown"/>

                <!-- ── Системный промпт ── -->
                <TextBlock Text="{x:Static res:Strings.AiCharacter_PromptLabel}"
                           Style="{StaticResource FieldLabel}"/>
                <TextBox x:Name="PromptBox"
                         Foreground="#FFF5D77F"
                         FontFamily="Segoe UI"
                         FontSize="12"
                         Height="160"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         VerticalContentAlignment="Top"
                         Margin="0,0,0,16"/>

                <!-- ── Кнопки: Отмена (Ruby) | Применить (Gold) ── -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0"
                            Click="OnCancelClick">
                        <TextBlock Text="{x:Static res:Strings.AiCharacter_Cancel}"
                                   Foreground="{StaticResource RubyBrush}"
                                   FontWeight="SemiBold"
                                   HorizontalAlignment="Center"/>
                    </Button>

                    <Button Grid.Column="2"
                            Click="OnApplyClick">
                        <TextBlock Text="{x:Static res:Strings.AiCharacter_Apply}"
                                   Foreground="{StaticResource GoldBrush}"
                                   FontWeight="SemiBold"
                                   HorizontalAlignment="Center"/>
                    </Button>
                </Grid>

            </StackPanel>
        </Border>
    </Border>
</Window>
